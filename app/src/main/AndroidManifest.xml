<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".GSDADashboardApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.alex.dashboarddemo.MainActivity"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Added by CleverTap Assistant-->
        <!-- Activities to be excluded from in-app notifications-->
        <meta-data
            android:name="CLEVERTAP_REGION"
            android:value="us1" />
        <!-- Adding CleverTap Credentials-->
        <meta-data
            android:name="CLEVERTAP_ACCOUNT_ID"
            android:value="TEST-6KK-857-9K6Z" />

        <meta-data
            android:name="CLEVERTAP_TOKEN"
            android:value="XXXXXX"/>
    </application>

</manifest>
